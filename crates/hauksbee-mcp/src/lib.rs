//! hauksbee-mcp: the Model Context Protocol server for hauksbee, the agent
//! surface monetization plan E calls the missing wedge. It speaks JSON-RPC 2.0
//! over stdio (newline-delimited, per the MCP stdio transport) and exposes the
//! engine's analysis, the CI spec runner, capability discovery, and the
//! Board-as-Code text form as structured tools, plus a QuickJS "code mode"
//! sandbox that composes those tools server-side in one call. The honesty
//! doctrine is load-bearing here exactly as it is in the CLI: an unanswerable
//! run returns a structured refusal, never a fabricated result.

pub mod protocol;
pub mod script;
pub mod tools;
