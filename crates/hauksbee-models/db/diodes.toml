# Diode models.
# SPICE params: is (A), n, rs (Ω), cjo (F), vj (V), m, tt (s), bv (V).

[[models]]
id = "1n4148"
kind = "diode"
description = "1N4148 small-signal switching diode, DO-35"

[models.match]
# Same small-signal switching die across SMD packages: 1N4148W (SOD-123),
# 1N4148WS (SOD-323), 1N4148WT, 1N4148-7-F. The distinct parts 1N4448 / 1N4150
# / 1N914 differ before the suffix and are correctly rejected.
value_re = "(?i)^1N4148[A-Z0-9-]*$"

[models.params]
# Source: Vishay 1N4148 datasheet Rev. 14, SPICE model parameters
is  = 2.52e-9   # saturation current
n   = 1.752     # emission coefficient
rs  = 0.568     # series resistance (Ω)
cjo = 4.0e-12   # zero-bias junction capacitance (F)
vj  = 0.8       # built-in potential (V)
m   = 0.4       # grading coefficient
tt  = 6.0e-9    # transit time (s)
bv  = 100.0     # reverse breakdown voltage (V)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 0.3        # IF repetitive peak (Vishay Rev 14)
max_surge_current_a = 2.0  # IFSM 1us
max_power_w = 0.5
max_voltage_v = 100.0      # VRRM

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4001"
kind = "diode"
description = "1N400x rectifier diode family fallback, DO-41 (each member has an explicit entry below that outranks this)"

[models.match]
# Family safety net. Every member 1N4002–1N4007 also has a dedicated entry
# with its real breakdown voltage; those win the tie because an exact literal
# regex is more constrained than this character class (see
# CompiledEntry::regex_specificity). In practice this entry only ever binds
# 1N4001 itself.
value_re = "(?i)^1N400[1-7]$"

[models.params]
# Source: Vishay 1N4001-1N4007 datasheet, Rev. 15, SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 50.0     # 1N4001 VRRM; higher-voltage members bind their own entries below

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0        # IF(AV)
max_surge_current_a = 30.0 # IFSM 8.3ms half-sine

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4002"
kind = "diode"
description = "1N4002 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4002$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 100.0   # 1N4002 rated 100 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 100.0      # VRRM 1N4002

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4003"
kind = "diode"
description = "1N4003 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4003$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 200.0   # 1N4003 rated 200 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 200.0      # VRRM 1N4003

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4004"
kind = "diode"
description = "1N4004 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4004$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 400.0   # 1N4004 rated 400 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 400.0      # VRRM 1N4004

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4005"
kind = "diode"
description = "1N4005 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4005$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 600.0   # 1N4005 rated 600 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 600.0      # VRRM 1N4005

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4006"
kind = "diode"
description = "1N4006 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4006$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 800.0   # 1N4006 rated 800 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 800.0      # VRRM 1N4006

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "1n4007"
kind = "diode"
description = "1N4007 rectifier diode, DO-41 (explicit match over 1N400x family)"

[models.match]
value_re = "(?i)^1N4007$"

[models.params]
# Source: ON Semiconductor 1N4001-1N4007 SPICE model
is  = 7.62e-9
n   = 1.984
rs  = 0.042
cjo = 2.0e-11
vj  = 0.75
m   = 0.333
tt  = 5.76e-6
bv  = 1000.0  # 1N4007 rated 1000 V PIV

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0
max_surge_current_a = 30.0
max_voltage_v = 1000.0     # VRRM 1N4007

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "bat43"
kind = "diode"
description = "BAT43 small-signal Schottky diode, 30 V, 200 mA, DO-35"

[models.match]
# BAT42 is the same die with a different forward grade, so it gets its own
# numbers rather than sharing these. The W and LL suffixes are the SOD-123 and
# MiniMELF packages of the same part.
value_re = "(?i)^BAT43(W(-V)?)?$|^LL43$"

[models.params]
# Source: Vishay BAT42/BAT43 datasheet, document 85660, Rev. 2.2, 12-May-2025.
# ELECTRICAL CHARACTERISTICS for the forward voltages, ABSOLUTE MAXIMUM RATINGS
# and THERMAL CHARACTERISTICS for the rest.
#
# Fitted, not quoted: the datasheet publishes no SPICE model. `is`, `n` and `rs`
# are a least-squares fit to the three BAT43 VF rows (330 mV at 2 mA, 450 mV at
# 15 mA, 1000 mV at 200 mA) and reproduce all three to 0.1 mV. Those are the MAX
# column, so this is a worst-case part: pessimistic against a typical one, which
# is the right direction for a checker.
is  = 1.329e-6
n   = 1.72
rs  = 2.35
cjo = 7.0e-12   # CD typ at VR = 1 V, f = 1 MHz
vj  = 0.4       # assumed barrier height; the datasheet gives none
m   = 0.4       # assumed grading coefficient, ditto
tt  = 1.0e-11   # majority-carrier device; trr max 5 ns
bv  = 30.0      # V(BR) min at IR = 100 uA

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 0.2             # IF, forward continuous current
max_surge_current_a = 4.0       # IFSM, tp < 10 ms
max_power_w = 0.2               # Ptot at Tamb = 65 C
max_voltage_v = 30.0            # VRRM
max_junction_temp_c = 125.0     # Tj
theta_ja_c_per_w = 300.0        # RthJA, junction to ambient air

# ──────────────────────────────────────────────────────────────────────────────

[[models]]
id = "bat54"
kind = "diode"
description = "BAT54 Schottky barrier diode, 30 V, 200 mA, SOT23"

[models.match]
# The plain single diode only. BAT54A, BAT54C and BAT54S are two diodes in one
# SOT23 with three different internal connections, so they need their own pin
# maps and must not silently bind here. BAT43 and the BAT4x parts are separate
# devices with their own forward curves.
value_re = "(?i)^BAT54(/DG)?$"

[models.params]
# Source: Nexperia BAT54 datasheet, 1 July 2022, Table 7 "Characteristics".
#
# Fitted, not quoted: that datasheet publishes no SPICE model. `is`, `n` and
# `rs` are a least-squares fit to the whole VF column, five points spanning
# 0.1 mA to 100 mA (240, 320, 400, 500 and 800 mV), and they reproduce it to
# 11 mV rms. Those are MAX figures with no typ column, so this models a
# worst-case part: it will read a little pessimistic against a typical one,
# which is the right direction for a checker.
is  = 4.834e-9
n   = 1.0
rs  = 3.6
cjo = 1.0e-11   # Cd max at VR = 1 V, f = 1 MHz, Table 7
vj  = 0.4       # assumed barrier height; the datasheet gives none
m   = 0.4       # assumed grading coefficient, ditto
tt  = 1.0e-11   # majority-carrier device, negligible stored charge; trr max 5 ns
bv  = 30.0      # VR, Table 5 "Limiting values"

[models.pins]
# Table 2 "Pinning information": SOT23 with the middle pin unconnected.
"1" = "anode"
"2" = "nc"
"3" = "cathode"
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 0.2             # IF at Tamb = 25 C, Table 5
max_surge_current_a = 0.6       # IFSM, tp < 10 ms, Table 5
max_power_w = 0.25              # Ptot at Tamb <= 25 C, Table 5
max_voltage_v = 30.0            # VR, Table 5
max_junction_temp_c = 150.0     # Tj, Table 5
theta_ja_c_per_w = 500.0        # Rth(j-a) in free air, Table 6

# ─────────────────────────────────────────────────────────────────────────────
# USBLC6-2P6: STMicroelectronics very-low-capacitance ESD protection array.
# Source: ST "USBLC6-2 Very low capacitance ESD protection" datasheet,
#   Doc ID 11265 Rev 5, October 2011 (st.com/resource/en/datasheet/usblc6-2.pdf).
# ST publishes a SPICE/IBIS deck for this part under a licence that does not
# permit redistribution, so nothing here is copied from it; every number below
# comes from the datasheet's own tables and its Section 2.1 worked example.
#
# TOPOLOGY (datasheet Figure 1 and Section 2.1, "rail to rail topology"):
# five junctions on one die, per I/O line a pair of steering diodes and one
# transil across the supply rails.
#
#            VBUS (pin 5)
#              |     |
#             ///   ///          two steering diodes, one per I/O, cathodes
#              |     |           to VBUS: I/O -> VBUS conducts on a positive
#          I/O1     I/O2         surge
#              |     |
#             ///   ///          two steering diodes, anodes on GND: GND ->
#              |     |           I/O conducts on a negative surge
#            GND (pin 2)
#              |
#            transil (VBR 6 V) between VBUS and GND, cathode on VBUS
#
# SOT-666 pinout (Figure 1, top view): 1=I/O1, 2=GND, 3=I/O2, 4=I/O2,
# 5=VBUS, 6=I/O1. Pins 1/6 are the same internal node, as are 3/4.
#
# Datasheet figures used (Table 2, Electrical characteristics, Tamb=25C):
#   IRM  = 10 nA typ, 150 nA max  @ VRM = 5.25 V   (the standoff voltage)
#   VBR  >= 6 V                   @ IR = 1 mA      (VBUS to GND)
#   VF   <= 1.1 V                 @ IF = 10 mA
#   VCL  <= 12 V @ IPP = 1 A, 8/20 us, any I/O pin to GND
#   VCL  <= 17 V @ IPP = 5 A, 8/20 us, any I/O pin to GND
#   Ci/o-GND = 2.5 pF typ, 3.5 pF max @ VR = 1.65 V
# Section 2.1 states the piecewise model ST itself uses:
#   VF = VT + Rd*Ip with VT = 1.1 V and Rd = 0.5 Ohm (steering diodes)
#   VTRANSIL = VBR + Rd_TRANSIL*Ip with VBR = 6.1 V and Rd_TRANSIL = 0.5 Ohm
#
# WHAT THIS ENTRY STAMPS. The binder stamps one SPICE diode per `diode` entry,
# so the anode/cathode roles are given to the transil (GND -> VBUS), which is
# the junction with DC consequence on a board: it clamps the 5 V rail at its
# breakdown. The four I/O steering diodes are stamped instead as behavioural
# clamp laws below, using ST's own piecewise numbers, so the whole five-junction
# topology is present rather than one fifth of it.
#
# WHAT THIS ENTRY DOES NOT STAMP, said plainly: the 2.5 pF I/O-to-GND line
# capacitance and the 150 nA leakage. Both are stated on the datasheet; neither
# is modelled, because the array has no per-I/O SPICE device to hang them on.
# The consequence is that USB eye-diagram / line-loading questions are NOT
# answered by this model. Everything the model does answer (rail clamping,
# surge conduction paths) is datasheet-backed.
[[models]]
id = "usblc6_2"
kind = "diode"
description = "USBLC6-2P6/2SC6 ST 2-line ESD array, 5.25V standoff, VBR 6V, rail-to-rail"

[models.match]
# Both package options of the same die: USBLC6-2P6 (SOT-666) and USBLC6-2SC6
# (SOT23-6L), plus the bare "USBLC6-2". No other ST part shares the prefix.
value_re = "(?i)^USBLC6-?2[A-Z0-9-]*$"

[models.params]
# The VBUS-to-GND transil. Source: ST USBLC6-2 datasheet Doc ID 11265 Rev 5.
# `n` is the one number here that the datasheet does not print. It is chosen as
# 2.0, an ordinary recombination-dominated emission coefficient, and `is` then
# follows from the single stated forward point: is = 10 mA / exp(1.095 V /
# (2 * 25.85 mV)) = 6.3e-12 A, where 1.095 V is the 1.1 V maximum less the
# 5 mV dropped across rs at 10 mA. Fitting the MAXIMUM forward drop is
# deliberate: it is the only forward figure the sheet guarantees.
is  = 6.3e-12   # from VF <= 1.1 V at IF = 10 mA with n = 2.0 (see above)
n   = 2.0       # NOT a datasheet value; see the note above
rs  = 0.5       # Rd_TRANSIL typ, datasheet Section 2.1
bv  = 6.0       # VBR min at IR = 1 mA (Section 2.1 uses 6.1 V as the typical)

# Parameters for the four steering-diode clamp laws further down, both from
# datasheet Section 2.1: the forward threshold VT and the dynamic resistance Rd
# that ST uses in its own clamping-voltage calculation. They live in `params`
# because that is where the behavioural runtime reads law variables from; the
# diode reader ignores keys it does not know.
vt_clamp = 1.1  # VT, forward threshold of a steering diode (V)
rd_clamp = 0.5  # Rd, dynamic resistance of a steering diode (Ohm)

[models.pins]
# SOT-666 / SOT23-6L, datasheet Figure 1 (top view).
# The transil is the diode the binder stamps, so GND carries the "anode" role
# and VBUS the "cathode" role: below breakdown that junction is reverse biased
# by VBUS, and it conducts forward if VBUS is driven below GND. Both are the
# real behaviour of the rail-to-rail transil.
"1" = "io1"       # I/O1
"2" = "anode"     # GND, anode of the VBUS-GND transil
"3" = "io2"       # I/O2
"4" = "io2"       # I/O2 (second pad, same internal node as pin 3)
"5" = "cathode"   # VBUS, cathode of the VBUS-GND transil
"6" = "io1"       # I/O1 (second pad, same internal node as pin 1)

[models.ratings]
# The datasheet publishes no continuous current or power rating for this part:
# it is a surge device rated by IEC 61000-4-2 pulse level, not by DC. Only the
# figures it does state appear here.
max_voltage_v = 5.25        # VRM, the reverse standoff voltage (leakage test condition)
max_junction_temp_c = 125.0 # Tj operating max, datasheet Table 1

# The four I/O steering diodes, as piecewise-linear clamp laws. Each conducts
# only once its junction is forward biased past VT, so in every normal operating
# condition (VBUS at 5 V, data lines between 0 V and 3.3 V) all four evaluate to
# exactly zero current and the part is the open circuit it really is. They come
# alive only when a line is driven above VBUS + 1.1 V or below GND - 1.1 V,
# which is the surge case ST's Section 2.1 describes.
# `v_anode` is the GND pin and `v_cathode` the VBUS pin; see the pin map above
# for why those role names carry the rails.

[[models.behavioral.laws]]
name = "io1_to_vbus"
kind = "current"
a = "io1"
b = "cathode"
expr = "max(0.0, (v_io1 - v_cathode - vt_clamp) / rd_clamp)"

[[models.behavioral.laws]]
name = "gnd_to_io1"
kind = "current"
a = "anode"
b = "io1"
expr = "max(0.0, (v_anode - v_io1 - vt_clamp) / rd_clamp)"

[[models.behavioral.laws]]
name = "io2_to_vbus"
kind = "current"
a = "io2"
b = "cathode"
expr = "max(0.0, (v_io2 - v_cathode - vt_clamp) / rd_clamp)"

[[models.behavioral.laws]]
name = "gnd_to_io2"
kind = "current"
a = "anode"
b = "io2"
expr = "max(0.0, (v_anode - v_io2 - vt_clamp) / rd_clamp)"

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "schottky_generic"
kind = "diode"
description = "Generic Schottky diode (value field contains SCHOTTKY)"

[models.match]
value_re = "(?i)SCHOTTKY"

[models.params]
# Reasonable Schottky defaults (low-power, ~30V rated)
is  = 1.0e-8
n   = 1.5
rs  = 2.0
cjo = 1.5e-12
vj  = 0.35
m   = 0.4
tt  = 5.0e-12
bv  = 30.0

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "led_red"
kind = "diode"
description = "Red LED, Vf ≈ 2.0 V @ 20 mA"

[models.match]
value_re = "(?i)RED.?LED|LED.?RED"

[models.params]
# Source: typical AlGaInP red LED parameters
is  = 1.0e-20
n   = 1.8
rs  = 6.0
cjo = 2.0e-12
vj  = 1.7
m   = 0.5
tt  = 1.0e-9
bv  = 5.0

[models.pins]
"K" = "cathode"
"A" = "anode"

[models.ratings]
max_current_a = 0.025      # typical 0603/0805 indicator LED IF max
max_surge_current_a = 0.1
max_voltage_v = 5.0  # reverse

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "led_green"
kind = "diode"
description = "Green LED, Vf ≈ 2.1 V @ 20 mA"

[models.match]
value_re = "(?i)GREEN.?LED|LED.?GREEN"

[models.params]
# Source: typical InGaN green LED parameters
is  = 1.0e-20
n   = 1.8
rs  = 5.0
cjo = 2.0e-12
vj  = 1.8
m   = 0.5
tt  = 1.0e-9
bv  = 5.0

[models.pins]
"K" = "cathode"
"A" = "anode"

[models.ratings]
max_current_a = 0.025
max_surge_current_a = 0.1
max_voltage_v = 5.0

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "led_blue"
kind = "diode"
description = "Blue LED, Vf ≈ 3.3 V @ 20 mA"

[models.match]
value_re = "(?i)BLUE.?LED|LED.?BLUE"

[models.params]
# Source: typical InGaN blue LED parameters
is  = 1.0e-22
n   = 2.0
rs  = 5.0
cjo = 2.0e-12
vj  = 2.8
m   = 0.5
tt  = 1.0e-9
bv  = 5.0

[models.pins]
"K" = "cathode"
"A" = "anode"

[models.ratings]
max_current_a = 0.025
max_surge_current_a = 0.1
max_voltage_v = 5.0

# ─────────────────────────────────────────────────────────────────────────────

[[models]]
id = "led_yellow"
kind = "diode"
description = "Yellow LED, Vf ≈ 2.1 V @ 20 mA"

[models.match]
value_re = "(?i)YELLOW.?LED|LED.?YELLOW"

[models.params]
# Source: typical AlGaInP yellow LED parameters
is  = 1.0e-20
n   = 1.8
rs  = 6.0
cjo = 2.0e-12
vj  = 1.8
m   = 0.5
tt  = 1.0e-9
bv  = 5.0

[models.pins]
"K" = "cathode"
"A" = "anode"

# ─────────────────────────────────────────────────────────────────────────────
# Generic LED (footprint-matched, when value alone is not enough)

[models.ratings]
max_current_a = 0.025
max_surge_current_a = 0.1
max_voltage_v = 5.0

[[models]]
id = "led_generic"
kind = "diode"
description = "Generic LED matched by footprint prefix"

[models.match]
footprint_re = "^LED_THT:|^LED_SMD:"

[models.params]
# Conservative defaults: Vf ≈ 2 V red-ish
is  = 1.0e-20
n   = 1.8
rs  = 6.0
cjo = 2.0e-12
vj  = 1.7
m   = 0.5
tt  = 1.0e-9
bv  = 5.0

[models.pins]
"K" = "cathode"
"A" = "anode"

[models.ratings]
max_current_a = 0.025
max_surge_current_a = 0.1
max_voltage_v = 5.0

# ─────────────────────────────────────────────────────────────────────────────
# Zener diodes, the 1N47xxA family: 1 W, DO-41 axial, Vz tolerance +/-5%.
#
# A Zener is an ordinary diode whose REVERSE breakdown is the point of the part,
# so `bv` carries the whole model and comes straight off the datasheet. The
# solver conducts below -bv with ngspice's own breakdown shape, so a shunt
# regulator or a clamp behaves rather than sitting open until something melts.
#
# The forward parameters are a FIT, not a table lookup, and are shared across
# the family: this datasheet gives one forward number, VF <= 1.2 V at
# IF = 200 mA, and no SPICE card. `is`, `n` and `rs` below put VF near 1.0 V at
# that current, inside the limit. Anyone who needs an accurate forward drop from
# a Zener is using the part backwards, and the comment says so rather than
# implying the numbers were read off a curve.
#
# Source for every Vz, Izt, Zzt and rating: Multicomp 1N4728A-1N4764A datasheet
# V1.1 (06/04/11), "Specification Table" and "Absolute Maximum Ratings".
#
# The family is listed as the datasheet prints it, which is abridged: it skips
# from 3.9 V to 4.7 V and from 10 V to 62 V. The missing codes are real parts,
# and they are left out rather than interpolated.

[[models]]
id = "1n4728a"
kind = "diode"
description = "1N4728A Zener diode 3.3 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4728[ABC]?$"

[models.params]
bv  = 3.3        # VZnom at IZT = 76 mA, Specification Table
ibv = 0.076      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 3.3      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4729a"
kind = "diode"
description = "1N4729A Zener diode 3.6 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4729[ABC]?$"

[models.params]
bv  = 3.6        # VZnom at IZT = 69 mA, Specification Table
ibv = 0.069      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 3.6      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4730a"
kind = "diode"
description = "1N4730A Zener diode 3.9 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4730[ABC]?$"

[models.params]
bv  = 3.9        # VZnom at IZT = 64 mA, Specification Table
ibv = 0.064      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 3.9      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4732a"
kind = "diode"
description = "1N4732A Zener diode 4.7 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4732[ABC]?$"

[models.params]
bv  = 4.7        # VZnom at IZT = 53 mA, Specification Table
ibv = 0.053      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 4.7      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4733a"
kind = "diode"
description = "1N4733A Zener diode 5.1 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4733[ABC]?$"

[models.params]
bv  = 5.1        # VZnom at IZT = 49 mA, Specification Table
ibv = 0.049      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 5.1      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4734a"
kind = "diode"
description = "1N4734A Zener diode 5.6 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4734[ABC]?$"

[models.params]
bv  = 5.6        # VZnom at IZT = 45 mA, Specification Table
ibv = 0.045      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 5.6      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4735a"
kind = "diode"
description = "1N4735A Zener diode 6.2 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4735[ABC]?$"

[models.params]
bv  = 6.2        # VZnom at IZT = 41 mA, Specification Table
ibv = 0.041      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 6.2      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4736a"
kind = "diode"
description = "1N4736A Zener diode 6.8 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4736[ABC]?$"

[models.params]
bv  = 6.8        # VZnom at IZT = 37 mA, Specification Table
ibv = 0.037      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 6.8      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4737a"
kind = "diode"
description = "1N4737A Zener diode 7.5 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4737[ABC]?$"

[models.params]
bv  = 7.5        # VZnom at IZT = 34 mA, Specification Table
ibv = 0.034      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 7.5      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4738a"
kind = "diode"
description = "1N4738A Zener diode 8.2 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4738[ABC]?$"

[models.params]
bv  = 8.2        # VZnom at IZT = 31 mA, Specification Table
ibv = 0.031      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 8.2      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4739a"
kind = "diode"
description = "1N4739A Zener diode 9.1 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4739[ABC]?$"

[models.params]
bv  = 9.1        # VZnom at IZT = 28 mA, Specification Table
ibv = 0.028      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 9.1      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4740a"
kind = "diode"
description = "1N4740A Zener diode 10.0 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4740[ABC]?$"

[models.params]
bv  = 10.0        # VZnom at IZT = 25 mA, Specification Table
ibv = 0.025      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 10.0      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

[[models]]
id = "1n4759a"
kind = "diode"
description = "1N4759A Zener diode 62.0 V, 1 W, DO-41"

[models.match]
# Accepts the bare code and the A/B/C tolerance suffixes; the voltage is set by
# the code, so a suffix cannot change which rail this clamps at.
value_re = "(?i)^1N4759[ABC]?$"

[models.params]
bv  = 62.0        # VZnom at IZT = 4 mA, Specification Table
ibv = 0.004      # the knee sits at IZT, so V(bv) is the datasheet VZ there
is  = 1.0e-9      # forward fit, shared across the family (see the note above)
n   = 1.6         # forward fit
rs  = 1.0         # forward fit; puts VF near 1.0 V at IF = 200 mA (1.2 V max)

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_power_w = 1.0            # Pv at Tamb <= 50 C, Absolute Maximum Ratings
max_voltage_v = 62.0      # reverse working voltage is the Zener voltage itself
max_junction_temp_c = 200.0  # Tj, Absolute Maximum Ratings
theta_ja_c_per_w = 100.0     # RthJA, Maximum Thermal Resistance

# ─────────────────────────────────────────────────────────────────────────────
# 1N581x Schottky barrier rectifiers, 1 A, DO-41.
#
# Source for every number: Vishay 1N5817/1N5818/1N5819 datasheet, document
# 88525, revision 01-Jul-2020. Ratings from "MAXIMUM RATINGS", forward voltages
# from "ELECTRICAL CHARACTERISTICS", junction capacitance from the same table,
# thermal resistance from "THERMAL CHARACTERISTICS".
#
# These are the parts that made the series resistance worth stamping. The
# datasheet gives TWO forward points, VF at 1.0 A and at 3.1 A, and the rise
# between them is bulk resistance rather than the exponential. Fitting both with
# rs = 0 needs an ideality factor above 10, four times anything physical and
# past what validation accepts; fitting one leaves the other a third out. With
# rs in the model, `is`, `n` and `rs` reproduce both published points exactly,
# which is what the ngspice deck schottky_1n5817_two_points checks.
#
# `n = 1.05` is the assumption the fit rests on, and it is the only one: it is
# an ordinary Schottky ideality factor, and the datasheet does not publish one.
# `is` and `rs` then follow from the two VF points with no freedom left.

[[models]]
id = "1n5817"
kind = "diode"
description = "1N5817 Schottky barrier rectifier, 20 V, 1 A, DO-41"

[models.match]
value_re = "(?i)^1N5817$"

[models.params]
is  = 7.105e-06     # fitted with n and rs to both forward points, see the note above
n   = 1.05      # assumed, an ordinary Schottky ideality factor
rs  = 0.1282     # fitted; carries the 1.0 A to 3.1 A rise the datasheet publishes
cjo = 1.25e-10    # CJ typ at VR = 4.0 V, 1.0 MHz
vj  = 0.4       # assumed, a Schottky barrier height; the datasheet gives none
m   = 0.4       # assumed grading coefficient, ditto
tt  = 1.0e-11   # majority-carrier device: negligible stored charge, assumed
bv  = 20.0      # VRRM, Maximum repetitive peak reverse voltage

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0             # IF(AV) at TL = 90 C
max_surge_current_a = 25.0      # IFSM, 8.3 ms single half sine-wave
max_voltage_v = 20.0            # VRRM
max_junction_temp_c = 125.0     # TJ maximum
theta_ja_c_per_w = 50.0         # RthJA typ, junction to ambient
theta_jc_c_per_w = 15.0         # RthJL typ, junction to lead

[[models]]
id = "1n5818"
kind = "diode"
description = "1N5818 Schottky barrier rectifier, 30 V, 1 A, DO-41"

[models.match]
value_re = "(?i)^1N5818$"

[models.params]
is  = 2.767e-07     # fitted with n and rs to both forward points, see the note above
n   = 1.05      # assumed, an ordinary Schottky ideality factor
rs  = 0.1401     # fitted; carries the 1.0 A to 3.1 A rise the datasheet publishes
cjo = 1.1e-10    # CJ typ at VR = 4.0 V, 1.0 MHz
vj  = 0.4       # assumed, a Schottky barrier height; the datasheet gives none
m   = 0.4       # assumed grading coefficient, ditto
tt  = 1.0e-11   # majority-carrier device: negligible stored charge, assumed
bv  = 30.0      # VRRM, Maximum repetitive peak reverse voltage

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0             # IF(AV) at TL = 90 C
max_surge_current_a = 25.0      # IFSM, 8.3 ms single half sine-wave
max_voltage_v = 30.0            # VRRM
max_junction_temp_c = 125.0     # TJ maximum
theta_ja_c_per_w = 50.0         # RthJA typ, junction to ambient
theta_jc_c_per_w = 15.0         # RthJL typ, junction to lead

[[models]]
id = "1n5819"
kind = "diode"
description = "1N5819 Schottky barrier rectifier, 40 V, 1 A, DO-41"

[models.match]
value_re = "(?i)^1N5819$"

[models.params]
is  = 2.828e-08     # fitted with n and rs to both forward points, see the note above
n   = 1.05      # assumed, an ordinary Schottky ideality factor
rs  = 0.1282     # fitted; carries the 1.0 A to 3.1 A rise the datasheet publishes
cjo = 1.1e-10    # CJ typ at VR = 4.0 V, 1.0 MHz
vj  = 0.4       # assumed, a Schottky barrier height; the datasheet gives none
m   = 0.4       # assumed grading coefficient, ditto
tt  = 1.0e-11   # majority-carrier device: negligible stored charge, assumed
bv  = 40.0      # VRRM, Maximum repetitive peak reverse voltage

[models.pins]
"A" = "anode"
"K" = "cathode"

[models.ratings]
max_current_a = 1.0             # IF(AV) at TL = 90 C
max_surge_current_a = 25.0      # IFSM, 8.3 ms single half sine-wave
max_voltage_v = 40.0            # VRRM
max_junction_temp_c = 125.0     # TJ maximum
theta_ja_c_per_w = 50.0         # RthJA typ, junction to ambient
theta_jc_c_per_w = 15.0         # RthJL typ, junction to lead
